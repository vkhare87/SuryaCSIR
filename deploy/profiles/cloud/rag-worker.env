# cloud (Supabase-hosted project + rented GPU/CPU inference) — ingestion worker.
# Copy to rag/.env, fill secrets, do NOT commit.
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
SUPABASE_ANON_KEY=
LLM_BACKEND=openllm
OPENLLM_BASE_URL=
OPENLLM_MODEL=qwen3-vl:8b
OCR_BACKEND=ollama
POLL_INTERVAL_S=30
BATCH_SIZE=5
