# institute server — copy to .env at repo root, fill Supabase anon key, do NOT commit.
# Built at deploy time (npm run build), served as static dist/ by nginx — this file
# only matters for the build step, not runtime.
VITE_SUPABASE_URL=http://localhost:8000
VITE_SUPABASE_ANON_KEY=
VITE_RAG_URL=/rag
