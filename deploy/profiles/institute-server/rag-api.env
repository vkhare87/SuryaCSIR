# institute server — query API. Copy to rag/.env.api, fill secrets, do NOT commit.
# No CORS_ORIGINS: nginx serves the SPA and proxies /rag/ under the same origin.
SUPABASE_URL=http://localhost:8000
SUPABASE_ANON_KEY=
LLM_BACKEND=openllm
OPENLLM_BASE_URL=http://localhost:11434/v1
OPENLLM_MODEL=qwen3-vl:32b
