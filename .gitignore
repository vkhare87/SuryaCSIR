node_modules/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store
.superpowers/
.gstack/

# Auto-generated SQL rollups (sources live in supabase/migrations + seed + mock)
supabase/bundles/

# Supabase CLI scratch state — per-machine link info (project ref, pooler URL)
# and cached remote version stamps. Not config: supabase/config.toml is the
# tracked source of truth. This directory outliving an uninstalled CLI is what
# made the repo look linked when it was not.
supabase/.temp/

# RAG worker (Python) local env
rag/.venv/
rag/.env
rag/.env.api
__pycache__/
.pytest_cache/
rag/eval/corpus.json
rag/eval/eval_report.md

# Ingest worker (Python) local env
ingest/.venv/
ingest/.env
rag/tok.tmp

# Extracted from the institute's licensing PDF: real commercial terms (licensees
# and amounts). Regenerate with rag/eval/extract_licensing.py — don't commit it.
scripts/licensing_rows.json
