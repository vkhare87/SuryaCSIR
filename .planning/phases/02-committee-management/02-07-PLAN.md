---
phase: 02-committee-management
plan: 07
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
  - 02-05
  - 02-06
files_modified:
  - src/App.tsx
  - src/components/layout/Layout.tsx
autonomous: true
requirements:
  - CMT-01

must_haves:
  truths:
    - "Navigating to /committees renders CommitteeList page"
    - "Navigating to /committees/:id renders CommitteeDetail page"
    - "Navigating to /committees/:id/meetings/:meetId renders MeetingDetail page"
    - "'Committees' link appears in sidebar for all authenticated users"
    - "Clicking the 'Committees' nav item navigates to /committees"
  artifacts:
    - path: "src/App.tsx"
      provides: "Route registration for 4 committee routes"
      contains: "/committees/:id/meetings/:meetId"
    - path: "src/components/layout/Layout.tsx"
      provides: "NAV_ITEMS entry for Committees with Building2 icon"
      contains: "/committees"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/committees/CommitteeList.tsx"
      via: "import CommitteeList"
      pattern: "import.*CommitteeList.*from.*committees/CommitteeList"
    - from: "src/App.tsx"
      to: "src/pages/committees/CommitteeDetail.tsx"
      via: "import CommitteeDetail"
      pattern: "import.*CommitteeDetail.*from.*committees/CommitteeDetail"
    - from: "src/App.tsx"
      to: "src/pages/committees/MeetingDetail.tsx"
      via: "import MeetingDetail"
      pattern: "import.*MeetingDetail.*from.*committees/MeetingDetail"
    - from: "src/components/layout/Layout.tsx"
      to: "NAV_ITEMS"
      via: "new entry with path /committees"
      pattern: "path: '/committees'"
---

<objective>
Wire the committee module into the application: register 4 React Router routes in App.tsx and add the Committees nav item to the sidebar.

Purpose: Without route registration and navigation, none of the committee pages built in Plans 02-02 through 02-06 are accessible. This plan connects everything (CMT-01, and enables all other CMT requirements by making their routes navigable).

Output: Modified App.tsx and Layout.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-committee-management/02-CONTEXT.md
@.planning/phases/02-committee-management/02-RESEARCH.md
@CLAUDE.md

<interfaces>
Existing App.tsx pattern (HashRouter, ProtectedRoute, Routes, Route):
  - Pages imported at top
  - Routes registered inside `<Route element={<Layout />}>` wrapper
  - More specific routes come before less specific routes (React Router ordering)
  - Role-based protection via `<ProtectedRoute allowedRoles={[...]}>`

Existing Layout.tsx pattern:
  - NAV_ITEMS array of { path, label, icon, allowedRoles }
  - ALL_ROLES constant for items visible to everyone
  - NavLink with active state styling
  - SidebarContent renders filteredNav (filtered by user's role)

Pitfall 6 (RESEARCH.md): Route ordering matters. Register:
  1. `/committees/:id/meetings/:meetId` (most specific)
  2. `/committees/:id/meetings`
  3. `/committees/:id/actions`
  4. `/committees/:id`
  5. `/committees` (least specific)

Actually, per D-02: routes are `/committees/:id` (Overview), `/committees/:id/meetings` (Meetings), `/committees/:id/actions` (Action Tracker), `/committees/:id/meetings/:meetId` (MeetingDetail).

The `/committees/:id/meetings` and `/committees/:id/actions` tabs are handled within CommitteeDetail.tsx using useLocation(). They do NOT need separate route entries — CommitteeDetail handles tab switching internally.

So the routes needed are:
  1. `/committees/:id/meetings/:meetId` → MeetingDetail (most specific, registered FIRST)
  2. `/committees/:id` → CommitteeDetail
  3. `/committees` → CommitteeList (least specific)

All routes are viewable by all authenticated users per the permissions matrix (CMT permissions table in design spec: "View all" = all roles).

CommitteeDetail handles `/committees/:id/meetings` and `/committees/:id/actions` internally via useLocation() tab detection — no additional route entries needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register committee routes in App.tsx</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (full file — route registration pattern, ProtectedRoute usage, import style, route ordering)
    - 02-RESEARCH.md Pitfall 6 (route ordering: specific before general)
    - src/pages/committees/CommitteeList.tsx (export default function CommitteeList)
    - src/pages/committees/CommitteeDetail.tsx (export default function CommitteeDetail)
    - src/pages/committees/MeetingDetail.tsx (export default function MeetingDetail)
  </read_first>
  <action>
    Edit `src/App.tsx` in 2 locations:

    **1. Add imports** — after the existing page imports (around line 34, after `import IrinsSync`), add:
    ```typescript
    import CommitteeList from './pages/committees/CommitteeList';
    import CommitteeDetail from './pages/committees/CommitteeDetail';
    import MeetingDetail from './pages/committees/MeetingDetail';
    ```

    **2. Register routes** — inside the `<Route element={<Layout />}>` block. Per Pitfall 6, register MOST specific routes FIRST. Add the 3 new routes BEFORE the `{/* Backstop */}` or catch-all route, but AFTER the existing specific routes.

    The committee routes should be placed logically near the end of the route list (before the catch-all), ordered as:

    ```tsx
    {/* Committee Management */}
    <Route path="/committees/:id/meetings/:meetId" element={<MeetingDetail />} />
    <Route path="/committees/:id" element={<CommitteeDetail />} />
    <Route path="/committees" element={<CommitteeList />} />
    ```

    All committee routes are viewable by ALL authenticated users per the permissions matrix ("View all" = all roles). Do NOT wrap them in ProtectedRoute with allowedRoles — the outer `<Route element={<ProtectedRoute />}>` already ensures authentication. The committee pages do NOT need additional role restrictions.

    Do NOT add routes for `/committees/:id/meetings` or `/committees/:id/actions` — those tabs are handled internally by CommitteeDetail.tsx via `useLocation()`.

    **Register order (Pitfall 6 — critical):**
    1. `/committees/:id/meetings/:meetId` (MOST specific — two params)
    2. `/committees/:id` (single param)
    3. `/committees` (no params — LEAST specific)

    Do NOT change any existing routes, imports, or component logic. Do NOT modify the ProtectedRoute component.
  </action>
  <acceptance_criteria>
    - `grep -c "import CommitteeList" src/App.tsx` returns 1
    - `grep -c "import CommitteeDetail" src/App.tsx` returns 1
    - `grep -c "import MeetingDetail" src/App.tsx` returns 1
    - `grep "committees/:id/meetings/:meetId" src/App.tsx` returns 1
    - `grep "committees/:id" src/App.tsx` returns 1
    - `grep "committees" src/App.tsx` returns at least 4 matches (3 imports + 3 routes)
    - The route for `/committees/:id/meetings/:meetId` appears BEFORE the route for `/committees/:id` in the file (Pitfall 6 — verify by checking line numbers)
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>3 committee routes registered in App.tsx with correct specificity order: MeetingDetail before CommitteeDetail before CommitteeList.</done>
</task>

<task type="auto">
  <name>Task 2: Add Committees nav item to Layout sidebar</name>
  <files>src/components/layout/Layout.tsx</files>
  <read_first>
    - src/components/layout/Layout.tsx (full file — NAV_ITEMS array lines 51-65, ALL_ROLES constant, Building2 icon already imported)
    - 02-CONTEXT.md canonical_refs (NAV_ITEMS pattern)
  </read_first>
  <action>
    Edit `src/components/layout/Layout.tsx` — add ONE entry to the `NAV_ITEMS` array.

    **Position:** Add after the existing Facilities/Instruments entry (line 57) and before Recruitment (line 58). Committees is a governance module that belongs between Instruments and Recruitment in the sidebar.

    **New entry:**
    ```typescript
    { path: '/committees',  label: 'Committees',      icon: Building2,       allowedRoles: ALL_ROLES },
    ```

    The `Building2` icon is already imported (line 14, used by Facilities). No new icon import needed.

    All authenticated users can view committees per the design spec permissions matrix. Use `ALL_ROLES` for `allowedRoles`.

    Do NOT add Helpdesk or any other nav items (Helpdesk is Phase 3). Do NOT change any existing NAV_ITEMS entries, ordering, or roles.
  </action>
  <acceptance_criteria>
    - `grep -c "path: '/committees'" src/components/layout/Layout.tsx` returns 1
    - `grep "Committees" src/components/layout/Layout.tsx` returns 1 (label string)
    - `grep "Building2" src/components/layout/Layout.tsx` returns at least 2 matches (existing Facilities import + new committees nav item)
    - The committees entry uses `allowedRoles: ALL_ROLES` (visible to all authenticated users)
    - The entry is between the Instruments/Facilities entry and the Recruitment entry in the array
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Committees nav item appears in sidebar for all authenticated users. Clicking navigates to /committees.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL -> React Router | User can type any committee URL; route matching and param validation handle it |
| client -> Layout.tsx | Nav items are filtered by role; role comes from AuthContext (Supabase Auth, not localStorage) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | Elevation of Privilege | Layout.tsx NAV_ITEMS | accept | Role-based nav filtering is UI convenience. Actual authorization is enforced by RLS on Supabase. A user typing `/committees` directly gets the page but RLS governs what data they see and can modify. |
| T-02-19 | Spoofing | React Router params | accept | Route params like `:id` are arbitrary strings. Detail pages handle invalid IDs with not-found UI (Plan 02-02, 02-03). No data leak or crash. |
</threat_model>

<verification>
- `npm run build` exits 0 (no type errors from route/page mismatches)
- `npm run lint` exits 0
- `/committees` route renders CommitteeList
- `/committees/:id` route renders CommitteeDetail
- `/committees/:id/meetings/:meetId` route renders MeetingDetail
- Sidebar shows "Committees" link for all authenticated users
- Route specificity order is correct (Pitfall 6)
</verification>

<success_criteria>
1. All 3 committee routes registered in App.tsx with correct specificity order
2. "Committees" appears in sidebar nav for all authenticated roles
3. Clicking "Committees" navigates to /committees (renders CommitteeList)
4. Navigating to /committees/:id renders CommitteeDetail
5. Navigating to /committees/:id/meetings/:meetId renders MeetingDetail
6. `npm run build` exits 0 with all 3 new page imports resolving correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-committee-management/02-07-SUMMARY.md`
</output>
