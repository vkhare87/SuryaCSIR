# Deferred Items — Phase 02, Plan 01

## Pre-existing Build Errors (out of scope, not fixed)

| File | Error | Severity |
|------|-------|----------|
| `src/pages/Facilities.tsx:180` | Cannot find name 'CalendarClock' | Build-blocking |
| `src/pages/Facilities.tsx:220` | Cannot find name 'CalendarClock' | Build-blocking |
| `src/pages/IrinsSync.tsx:301` | Type 'unknown' not assignable to 'ReactNode' | Build-blocking |

These pre-existed before Plan 02-01 execution. Per executor rules, pre-existing failures in unrelated files are out of scope and not fixed.
