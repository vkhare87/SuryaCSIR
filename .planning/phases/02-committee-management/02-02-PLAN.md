---
phase: 02-committee-management
plan: 02
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - src/pages/committees/CommitteeList.tsx
  - src/pages/committees/CommitteeDetail.tsx
autonomous: true
requirements:
  - CMT-01
  - CMT-03

must_haves:
  truths:
    - "User navigates to /committees and sees a card grid of committees with search bar and type/status filter pills"
    - "Filtering by 'Standing' type shows only Standing committees in the grid"
    - "Clicking a committee card navigates to /committees/:id showing the Overview tab"
    - "Overview tab shows: committee info, chairperson card, secretary card, members list, mini meeting timeline (last 3), action item counts"
    - "Meetings tab at /committees/:id/meetings lists all meetings for that committee"
    - "Action Tracker tab at /committees/:id/actions shows placeholder text (kanban wired in Plan 02-06)"
    - "Invalid committee ID shows not-found UI with back button"
  artifacts:
    - path: "src/pages/committees/CommitteeList.tsx"
      provides: "Committee list page with card grid, search, type/status filters"
      exports: ["default"]
    - path: "src/pages/committees/CommitteeDetail.tsx"
      provides: "Committee detail page with Overview/Meetings/Action Tracker tabs"
      exports: ["default"]
  key_links:
    - from: "src/pages/committees/CommitteeList.tsx"
      to: "useData().committees"
      via: "import { useData } from '../../contexts/DataContext'"
      pattern: "useData\\(\\)\\.committees"
    - from: "src/pages/committees/CommitteeList.tsx"
      to: "src/lib/committees/permissions.ts"
      via: "canCreateCommittee import"
      pattern: "import.*canCreateCommittee.*from.*permissions"
    - from: "src/pages/committees/CommitteeDetail.tsx"
      to: "useData().committees, useData().meetings, useData().actionItems, useData().committeeMembers"
      via: "useData() destructuring"
      pattern: "const.*committees.*meetings.*actionItems.*committeeMembers.*=.*useData"
    - from: "src/pages/committees/CommitteeDetail.tsx"
      to: "src/lib/committees/permissions.ts"
      via: "canEditCommittee import"
      pattern: "import.*canEditCommittee.*from.*permissions"
---

<objective>
Build the two core committee pages: CommitteeList (card grid with search and filters) and CommitteeDetail (tabs: Overview, Meetings, Action Tracker).

Purpose: These are the primary navigation pages for the committee module. CommitteeList is the entry point (CMT-01). CommitteeDetail provides tabbed navigation into all committee sub-views (CMT-03).

Output: Two new page components in `src/pages/committees/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-committee-management/02-CONTEXT.md
@.planning/phases/02-committee-management/02-RESEARCH.md
@CLAUDE.md

<interfaces>
Pattern: Facilities.tsx (list page) — search, filter, card/table grid, KPI cards, loading skeleton, empty state
Pattern: InstrumentDetail.tsx (detail page) — useParams, useData, useMemo, not-found UI, back nav

Existing DataContext exports (post Plan 02-01):
  useData() returns { committees[], meetings[], actionItems[], committeeMembers[], meetingDocs[], staff[], isLoading, ... }

Existing permissions (post Plan 02-01):
  canViewCommittees, canCreateCommittee, canEditCommittee, canDeleteCommittee, canScheduleMeeting, canWriteMinutes, canEditActionItems

Existing UI components:
  Card, Badge, StatCard from src/components/ui/Cards
  Skeleton from src/components/ui/Skeleton
  Button from src/components/ui/Button

Existing lucide-react icons: Building2, Search, Users, Calendar, CheckCircle2, Clock, AlertTriangle, Plus, ArrowLeft, Edit, Filter, GripVertical, X, Pencil, Check

Committee types:
  committee_type: 'Standing' | 'AdHoc' | 'Review' | 'Advisory'
  status: 'Active' | 'Inactive'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CommitteeList page with card grid, search, and filters</name>
  <files>src/pages/committees/CommitteeList.tsx</files>
  <read_first>
    - src/pages/Facilities.tsx (full file — study useData, useAuth, useMemo, useState, search/filter UI, card/table pattern, KPI cards, empty state, loading skeleton)
    - src/components/ui/Cards.tsx (Card, Badge, StatCard exports and their props)
    - src/components/ui/Skeleton.tsx (CardSkeleton and Skeleton exports)
    - src/components/ui/Button.tsx (Button component and variant props)
    - src/contexts/AuthContext.tsx (lines using useAuth — check export signature for { user, role } vs { user })
    - src/lib/committees/permissions.ts (canCreateCommittee function signature)
    - src/types/index.ts (Committee interface lines 222-232)
    - CLAUDE.md (coding rules: semantic tokens, no raw colors, import order, naming conventions)
  </read_first>
  <action>
    Create the directory `src/pages/committees/` if it does not exist.

    Create `src/pages/committees/CommitteeList.tsx` as `export default function CommitteeList()`.

    Follow the Facilities.tsx pattern (import order, useData/useAuth, useMemo for derived state, useState for filters) with these specific differences per CONTEXT.md D-03:

    **Header section:**
    - Title: "Committees"
    - Subtitle: "Institute governance committees and meeting management"
    - "Create Committee" button (Plus icon) only shown when `canCreateCommittee(user)` returns true (import from permissions.ts)

    **KPI cards row** (replacing Facilities' equipment KPIs):
    - Total committees (count)
    - Active (committee.status === 'Active')
    - Inactive (committee.status === 'Inactive')
    - Meetings (total across all committees — count meetings array length)
    - Pending Actions (actionItems with status === 'Pending')
    Use `KpiCard` component (same as Facilities.tsx imports from `../components/ui/KpiCard`).

    **Filter bar** (D-03):
    - Search input: placeholder "Search committees...", filters by committee.name (case-insensitive)
    - Type filter select: options "All Types", "Standing", "Ad Hoc" (value "AdHoc"), "Review", "Advisory"
    - Status filter select: options "All Status", "Active", "Inactive"

    **Card grid** (2-3 column responsive, D-03):
    Use `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`.
    Each card (clickable, navigates to `/committees/${c.id}`):
    - Card component wrapping a `<button>` with `className="text-left"`
    - Top row: committee name (h3, font-serif, font-[500]) + type Badge
      - Badge variant: 'info' for Standing, 'neutral' for others
    - Middle: mandate text (text-sm, text-text-muted, line-clamp-2)
    - Bottom row: status Badge + "Formed: {formed_date}" text
      - Badge variant: 'success' for Active, 'warning' for Inactive
    - Use `key={c.id}` on the wrapping button, NOT on Card

    **Loading state** (D-16):
    Show 6 CardSkeleton placeholders in the card grid while `isLoading` is true.

    **Empty state** (D-13):
    Centered layout (flex-col, items-center, justify-center, min-h-[50vh]):
    - Building2 icon (size 48, text-text-muted)
    - Message: "No committees configured yet."
    - "Create Committee" button (only if canCreateCommittee)
    - Use `Button` component with `variant="primary"`

    **Permissions:** Import `canCreateCommittee` from `../../lib/committees/permissions`. Show Create/Cancel buttons conditionally.

    **Semantic tokens:** Use only `bg-surface`, `text-text`, `text-text-muted`, `border-border`, `bg-surface-hover`. Never raw colors like `bg-white` or `text-gray-500`.
  </action>
  <acceptance_criteria>
    - `src/pages/committees/CommitteeList.tsx` exists
    - `grep -c "export default function CommitteeList" src/pages/committees/CommitteeList.tsx` returns 1
    - `grep -c "useData" src/pages/committees/CommitteeList.tsx` returns 1 (follows pattern: pages only call useData)
    - `grep -c "useAuth" src/pages/committees/CommitteeList.tsx` returns 1
    - `grep -c "useMemo" src/pages/committees/CommitteeList.tsx` returns >= 2 (filtered committee list + KPI calculations)
    - `grep -c "canCreateCommittee" src/pages/committees/CommitteeList.tsx` returns >= 2 (import + conditional usage)
    - `grep "Search" src/pages/committees/CommitteeList.tsx` returns at least 1 match (search input with Search icon)
    - `grep "Building2" src/pages/committees/CommitteeList.tsx` returns at least 1 match (empty state icon)
    - `grep -c "CardSkeleton" src/pages/committees/CommitteeList.tsx` returns >= 1 (loading state)
    - `grep -c "grid-cols-1 sm:grid-cols-2 lg:grid-cols-3" src/pages/committees/CommitteeList.tsx` returns 1
    - Contains ZERO raw color classes (no `bg-white`, `text-gray-500`, `text-blue-700`, etc.)
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>CommitteeList renders card grid with search + type/status filter. Empty state shows with CTA button (admin only). Loading state shows skeleton cards.</done>
</task>

<task type="auto">
  <name>Task 2: Create CommitteeDetail page with 3 tabs</name>
  <files>src/pages/committees/CommitteeDetail.tsx</files>
  <read_first>
    - src/pages/InstrumentDetail.tsx (full file — detail page pattern: useParams, useData, useMemo, not-found UI, back button, Card/Badge usage)
    - src/contexts/AuthContext.tsx (useAuth export signature)
    - src/lib/committees/permissions.ts (canEditCommittee, canScheduleMeeting, canWriteMinutes, canManageMembers signatures)
    - src/types/index.ts (Committee, Meeting, ActionItem, CommitteeMember, StaffMember interfaces)
    - src/components/ui/Cards.tsx (Card, Badge, StatCard)
    - CLAUDE.md (coding rules, semantic tokens)
  </read_first>
  <action>
    Create `src/pages/committees/CommitteeDetail.tsx` as `export default function CommitteeDetail()`.

    This page renders at `/committees/:id` (Overview tab), with sub-route content at `/committees/:id/meetings` (Meetings tab) and `/committees/:id/actions` (Action Tracker tab) per D-02.

    Follow the InstrumentDetail.tsx pattern.

    **Route params:** `const { id } = useParams<{ id: string }>()`
    If `id` is undefined or params detection is needed via `useParams`, destructure directly.

    **Data fetching:**
    ```typescript
    const { committees, meetings, actionItems, committeeMembers, staff, isLoading } = useData();
    const { user } = useAuth();
    ```

    **Derived state (all useMemo):**
    - `committee`: find by `c.id === id`
    - `chairperson`: find staff by `s.ID === committee.chairperson_id`
    - `secretary`: find staff by `s.ID === committee.secretary_id`
    - `members`: filter committeeMembers by `cm.committee_id === id`, join with staff for name/designation
    - `committeeMeetings`: filter meetings by `m.committee_id === id`, sorted by meeting_date descending
    - `committeeActions`: filter actionItems by checking if the action's meeting_id belongs to a meeting of this committee (or source is 'manual' for cross-committee items — for now, filter by checking meeting_id is in committeeMeetings IDs or source === 'manual' and assigned to someone on committee)
      - SIMPLER APPROACH per D-09/D-11: The Action Tracker at `/committees/:id/actions` shows items scoped to this committee. Filter actionItems where `meeting_id` is in `committeeMeetings.map(m => m.id)` OR `source === 'manual'`.
    - `actionCounts`: { pending, inProgress, completed } counts from committeeActions

    **Not-found state:**
    If `!committee` and `!isLoading`, show centered UI:
    - "Committee not found." text (text-text-muted)
    - "Back to Committees" button that navigates to `/committees`

    **Tab detection:**
    Use `useLocation()` from react-router-dom. Check `location.pathname`:
    - ends with `/meetings` → active tab "meetings"
    - ends with `/actions` → active tab "actions"
    - otherwise → active tab "overview"

    **Header / Back nav:**
    Same pattern as InstrumentDetail.tsx: ArrowLeft button → navigate('/committees'), title "Committee Profile", subtitle showing committee.committee_type + "Committee". "Edit" button (if canEditCommittee) for future form modal wiring (just show the button, onClick can be `() => {}` placeholder for now — the modal is wired in Plan 02-04).

    **Overview Tab (D-04) — rendered when active tab is "overview":**

    Layout: grid-cols-1 lg:grid-cols-3 gap-6

    LEFT COLUMN (lg:col-span-1):
    1. **Committee Info Card** (Card with border-t-4 border-[#c96442]):
       - Centered Building2 icon in circle (like InstrumentDetail identity card)
       - Committee name (h2, font-serif)
       - Type Badge + Status Badge below
    2. **Details Card:**
       - "Details" section header (10px font, uppercase tracking, text-[#c96442])
       - InfoRow pairs (from InstrumentDetail pattern — create a local InfoRow helper):
         - Type: committee.committee_type
         - Mandate: committee.mandate
         - Formed: committee.formed_date
         - Status: committee.status

    RIGHT COLUMN (lg:col-span-2):
    3. **Leadership Card:**
       - Chairperson: avatar circle (first letter) + name + designation + link to /staff/:id
       - Secretary: same pattern
    4. **Members Card:**
       - Section header "Members ({count})"
       - List of members with role badge (Member/Invitee/ExternalExpert)
       - Role badge: 'info' for Member, 'neutral' for Invitee, 'warning' for ExternalExpert
    5. **Mini Meeting Timeline** (Card):
       - Section header "Recent Meetings"
       - Show last 3 meetings from committeeMeetings (sorted by date descending)
       - Each: title, date, status badge
       - "View All Meetings" link that switches to meetings tab
    6. **Action Item Status Counts** (3 StatCards in a row):
       - Pending count | In Progress count | Completed count

    **Meetings Tab (D-02) — rendered when active tab is "meetings":**
    - List of all committeeMeetings sorted by meeting_date descending
    - Empty state (D-14): "No meetings scheduled" + "Schedule Meeting" button (shown if canScheduleMeeting(user, committee))
    - Each meeting row (Card or simple div):
      - Title (clickable → navigates to `/committees/${id}/meetings/${m.id}`)
      - Date, venue, status badge
    - "Schedule Meeting" button at top if permitted (placeholder onClick — wired in Plan 02-04)

    **Action Tracker Tab (D-02) — rendered when active tab is "actions":**
    - Currently a placeholder section: "Action Tracker — kanban board will be integrated in Plan 02-06"
    - Show simple list of committeeActions for now:
      - Each item: task text, assignee (find staff by ID), deadline, status badge
    - Empty state (D-14): "No action items" + "Create Action Item" button placeholder
    - "Create Action Item" and "New Action Item" button shown if canEditActionItems(user)

    **Tab Navigation Bar:**
    Render a tab bar below the header using NavLink components:
    - Overview: `/committees/${id}` (end prop)
    - Meetings: `/committees/${id}/meetings`
    - Action Tracker: `/committees/${id}/actions`
    Styling: flex row, active tab has border-b-2 border-[#c96442] + text-[#c96442], inactive has text-text-muted

    **Loading state:** Show Skeleton components while isLoading.
  </action>
  <acceptance_criteria>
    - `src/pages/committees/CommitteeDetail.tsx` exists
    - `grep -c "export default function CommitteeDetail" src/pages/committees/CommitteeDetail.tsx` returns 1
    - `grep -c "useParams" src/pages/committees/CommitteeDetail.tsx` returns 1
    - `grep -c "useLocation" src/pages/committees/CommitteeDetail.tsx` returns 1
    - `grep -c "useMemo" src/pages/committees/CommitteeDetail.tsx` returns >= 3
    - `grep -c "canEditCommittee\|canScheduleMeeting\|canEditActionItems\|canManageMembers" src/pages/committees/CommitteeDetail.tsx` returns >= 2 (imports + usages)
    - `grep "Committee not found" src/pages/committees/CommitteeDetail.tsx` returns 1 (not-found state)
    - `grep -c "NavLink\|nav-link\|navLink" src/pages/committees/CommitteeDetail.tsx` returns >= 3 (3 tab NavLinks)
    - `grep "/committees/" src/pages/committees/CommitteeDetail.tsx` returns >= 4 (navigation paths)
    - Contains ZERO raw color classes
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>CommitteeDetail renders Overview mini-dashboard at /committees/:id, Meetings list at /committees/:id/meetings, Action Tracker placeholder at /committees/:id/actions. Not-found state shows for invalid IDs. Tab navigation works via NavLink.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Supabase PostgreSQL | Pages read data via useData() context (pre-loaded). No direct Supabase calls from these pages. |
| URL -> React Router | Route params and location.pathname drive tab selection. Untrusted: user can type any :id. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | CommitteeDetail | accept | All committee data is readable by all authenticated users per RLS policy. No additional filtering needed. |
| T-02-05 | Denial of Service | CommitteeDetail | accept | Invalid :id param shows not-found UI; no crash, no data leak. |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run lint` exits 0
- `grep -r "export default function CommitteeList" src/pages/committees/` confirms page file
- `grep -r "export default function CommitteeDetail" src/pages/committees/` confirms page file
- Both pages use `useData()` and `useAuth()` (no direct Supabase calls)
- All derived state wrapped in `useMemo`
</verification>

<success_criteria>
1. `/committees` renders card grid with search and type/status filter
2. KPI cards show correct counts
3. Empty state shows with contextual CTA
4. Loading state shows skeleton placeholders
5. `/committees/:id` renders Overview tab with full mini-dashboard
6. Tab navigation switches between Overview, Meetings, Action Tracker
7. Meetings tab lists all meetings with status badges
8. Invalid committee ID shows not-found UI
9. `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-committee-management/02-02-SUMMARY.md`
</output>
