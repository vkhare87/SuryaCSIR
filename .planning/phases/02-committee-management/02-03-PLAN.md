---
phase: 02-committee-management
plan: 03
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - src/pages/committees/MeetingDetail.tsx
autonomous: true
requirements:
  - CMT-04
  - CMT-06
  - CMT-07

must_haves:
  truths:
    - "User navigates to /committees/:id/meetings/:meetId and sees stacked single-column meeting detail"
    - "Sections rendered in order: Meeting info card, Agenda items (numbered list), Minutes (read-only display), Action items (compact list), Documents (upload + list)"
    - "Agenda items display as numbered list with description and proposer name; Edit Agenda button shown for permitted users (wired in Plan 02-05)"
    - "Minutes display as read-only plain text with a placeholder noting full editor is wired in Plan 02-05"
    - "Minutes lock indicator shows when meeting is Completed + 7 days (amber Locked badge, text read-only)"
    - "Action items table shows task, assignee, deadline, status badge — status badge is clickable to cycle Pending->InProgress->Completed->Pending"
    - "Document section shows uploaded documents with download links; Upload button placeholder (wired in Plan 02-05)"
    - "Invalid meeting ID shows not-found UI"
  artifacts:
    - path: "src/pages/committees/MeetingDetail.tsx"
      provides: "Full meeting detail page with info, agenda (view mode), minutes (read-only), actions, documents"
      exports: ["default"]
  key_links:
    - from: "src/pages/committees/MeetingDetail.tsx"
      to: "useData()"
      via: "useData destructuring for meetings, agendaItems, actionItems, meetingDocs, committees, staff"
      pattern: "const.*meetings.*agendaItems.*actionItems.*meetingDocs.*=.*useData"
    - from: "src/pages/committees/MeetingDetail.tsx"
      to: "supabaseClient"
      via: "import { supabase } for action item status updates"
      pattern: "import.*supabase.*from.*supabaseClient"
    - from: "src/pages/committees/MeetingDetail.tsx"
      to: "src/lib/committees/permissions.ts"
      via: "canScheduleMeeting, canEditActionItems, canUnlockMinutes imports"
      pattern: "import.*canScheduleMeeting.*canEditActionItems.*canUnlockMinutes"
---

<objective>
Build the MeetingDetail page — a stacked single-column page showing meeting info, agenda items (view mode), a read-only minutes section, action items, and documents. Note: The interactive MinutesEditor, AgendaEditor, and DocumentUploader components from Plan 02-05 replace the placeholder sections via a wiring task. This plan renders the base structure with functional action item management and read-only displays for sections whose full interactivity is wired in Plan 02-05.

Purpose: MeetingDetail is where the core committee work happens — viewing agenda, reading minutes, tracking action items, and managing documents. CMT-04 (agenda view), CMT-06 (documents), and CMT-07 (action items) are partially addressed here; full CMT-05 (minutes editing) and CMT-04 (agenda reorder) are completed when Plan 02-05 wires the interactive components.

Output: `src/pages/committees/MeetingDetail.tsx`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-committee-management/02-CONTEXT.md
@.planning/phases/02-committee-management/02-RESEARCH.md
@CLAUDE.md

<interfaces>
Pattern: InstrumentDetail.tsx — useParams, useData, useMemo, not-found UI, InfoRow helper, Card/Badge

Route params: `{ id: string, meetId: string }` from useParams

DataContext arrays (post Plan 02-01):
  committees[], meetings[], actionItems[], committeeMembers[], meetingDocs[], agendaItems[], staff[]

Permissions (post Plan 02-01):
  canScheduleMeeting(user, committee), canEditActionItems(user), canUnlockMinutes(user)

Supabase client: `import { supabase } from '../../utils/supabaseClient'`

Meeting type fields: id, committee_id, meeting_date, venue, title, summary, status (Scheduled|Completed|Cancelled), created_at

AgendaItem type fields: id, meeting_id, sequence, description, proposed_by, status (Pending|Discussed|Deferred)

ActionItem type fields: id, meeting_id, source, task, assigned_to, deadline, status (Pending|InProgress|Completed), completed_at, notes

MeetingDocument type fields: id, meeting_id, file_name, storage_path, uploaded_at

StaffMember type fields: ID, Name, Designation, Division, etc.

D-17: Stacked single-column layout order: Info -> Agenda -> Minutes -> Action Items -> Documents
D-18: Agenda edit mode (placeholder — full editor in Plan 02-05 via AgendaEditor)
D-19: Minutes lock display — when Completed + 7 days, show amber Locked badge. Full MinutesEditor wired in Plan 02-05.
D-20: Document upload placeholder (full uploader in Plan 02-05 via DocumentUploader)
D-12: Status toggle on action items — click status badge to cycle Pending->InProgress->Completed->Pending

**Important:** The Minutes section in this plan is a SIMPLE READ-ONLY DISPLAY. It does NOT include the autosave textarea, lock logic, or unlock button — those live in the MinutesEditor component created by Plan 02-05. Plan 02-05 includes a wiring task that replaces this placeholder with the full MinutesEditor. The lock indicator (amber badge) IS rendered here since it is decorational and depends only on read fields.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MeetingDetail page with all 5 sections</name>
  <files>src/pages/committees/MeetingDetail.tsx</files>
  <read_first>
    - src/pages/InstrumentDetail.tsx (full file — detail page pattern, InfoRow helper, useParams, useMemo, not-found)
    - src/pages/committees/CommitteeDetail.tsx (created in Plan 02-02 — tab pattern for reference)
    - src/contexts/DataContext.tsx (useData return type, refreshData export)
    - src/contexts/AuthContext.tsx (useAuth export signature)
    - src/lib/committees/permissions.ts (canScheduleMeeting, canEditActionItems, canUnlockMinutes)
    - src/types/index.ts (Meeting, AgendaItem, ActionItem, MeetingDocument, StaffMember, Committee interfaces)
    - src/utils/supabaseClient.ts (supabase singleton export)
    - src/components/ui/Cards.tsx (Card, Badge)
    - CLAUDE.md (semantic tokens, import order, no raw colors)
  </read_first>
  <action>
    Create `src/pages/committees/MeetingDetail.tsx` as `export default function MeetingDetail()`.

    **Route params:**
    ```typescript
    const { id, meetId } = useParams<{ id: string; meetId: string }>();
    ```

    **Data and auth:**
    ```typescript
    const { committees, meetings, agendaItems, actionItems, meetingDocs, staff, isLoading, refreshData } = useData();
    const { user } = useAuth();
    ```

    **Derived state (all useMemo):**
    - `meeting`: find meetings by `m.id === meetId`
    - `committee`: find committees by `c.id === meeting?.committee_id` (or `id` param)
    - `meetingAgendaItems`: filter agendaItems by `a.meeting_id === meetId`, sorted by `sequence` ascending
    - `meetingActions`: filter actionItems by `a.meeting_id === meetId`
    - `meetingDocs`: filter meetingDocs by `d.meeting_id === meetId`
    - `isLocked` (D-19): `meeting.status === 'Completed' && new Date(meeting.meeting_date) < new Date(Date.now() - 7 * 86400000)`
    - `canUnlock`: `isLocked && user && canUnlockMinutes(user)` (import canUnlockMinutes from permissions)

    **Helper function for finding staff name:**
    ```typescript
    const staffName = (staffId: string): string => {
      const s = staff.find(st => st.ID === staffId);
      return s ? s.Name : staffId;
    };
    ```

    **Not-found state:**
    If `!meeting` and `!isLoading`, show centered: "Meeting not found." + "Back to Committee" button navigating to `/committees/${id}`.

    **Page layout (D-17):** Single-column stacked sections, each in a Card or similar container.

    ---

    **Section 1: Meeting Info Card**
    - Back button (ArrowLeft) + title "Meeting Detail"
    - Card with:
      - Title (h2, font-serif): meeting.title
      - Grid of info rows: Date (meeting.meeting_date), Venue (meeting.venue), Status (Badge: 'info' for Scheduled, 'success' for Completed, 'warning' for Cancelled)
      - If meeting.status === 'Completed' and meeting.summary has text: small note "Minutes recorded"
      - If meeting.status !== 'Completed': small note "Awaiting minutes"

    ---

    **Section 2: Agenda Items (view mode only — D-18)**

    Header: "Agenda" with item count badge.

    View mode content (full editor with drag-to-reorder wired in Plan 02-05):
    - Ordered list (`<ol className="list-decimal list-inside">`)
    - Each item: `<li>` with description text + proposer name in parentheses (text-text-muted, text-xs)
    - Empty state: "No agenda items" text
    - "Edit Agenda" button at bottom (Pencil icon) — shown if user has scheduling permission. Placeholder: `onClick` that is a no-op with a comment `// Wired by Plan 02-05 (AgendaEditor)`. The Plan 02-05 wiring task will replace this placeholder.

    ---

    **Section 3: Minutes (read-only display — D-19)**

    Header: "Meeting Minutes" with lock badge if locked.

    NOTE: This section is a SIMPLE READ-ONLY DISPLAY. The interactive MinutesEditor (textarea, autosave, unlock button) is created by Plan 02-05 and wired in via a task in that plan. This section renders the minutes text and lock indicator only.

    Render:
    - If `meeting.summary` has content: render it as `<p className="text-sm text-text whitespace-pre-wrap">{meeting.summary}</p>`
    - If `!meeting.summary`: render "No minutes recorded yet." (text-text-muted, italic)
    - If `isLocked`: show amber lock badge above the text:
      ```
      <span className="inline-flex items-center gap-1 px-2 py-0.5 bg-amber-50 border border-amber-200 rounded text-xs text-amber-700 mb-2">
        <Lock size={12} /> Locked — minutes are frozen 7 days after completion
      </span>
      ```
    - If `canUnlock`: show "Unlock" button (only in locked state) that calls the unlock RPC:
      ```typescript
      const handleUnlock = async () => {
        await supabase!.rpc('unlock_meeting_minutes', { p_meeting_id: meetId });
        await refreshData();
      };
      ```
    - Add a comment at the top of this section: `{/* Minutes section: read-only display. Interactive MinutesEditor wired by Plan 02-05. */}`

    Import `Lock` from lucide-react.
    Import `supabase` from `../../utils/supabaseClient`.
    Import `canUnlockMinutes` from `../../lib/committees/permissions`.

    ---

    **Section 4: Action Items (Compact List)**

    Header: "Action Items" with count badge.

    For each meetingAction:
    - Card row with:
      - Task description (text-sm, font-medium)
      - Assignee name (from staffName helper)
      - Deadline (formatted date)
      - Status badge — CLICKABLE per D-12:
        ```typescript
        const cycleStatus = async (item: ActionItem) => {
          const next: Record<string, ActionItem['status']> = {
            Pending: 'InProgress',
            InProgress: 'Completed',
            Completed: 'Pending',
          };
          const newStatus = next[item.status];
          await supabase!.from('action_items')
            .update({ status: newStatus, completed_at: newStatus === 'Completed' ? new Date().toISOString() : null })
            .eq('id', item.id);
          await refreshData();
        };
        ```
        Only allow click if `canEditActionItems(user)` returns true.
      - Badge variant: 'warning' for Pending, 'info' for InProgress, 'success' for Completed

    Empty state: "No action items for this meeting."

    Import `canEditActionItems` from `../../lib/committees/permissions`.

    ---

    **Section 5: Documents (Upload placeholder + List)**

    Header: "Documents" with count badge.

    Upload button placeholder (full DocumentUploader wired in Plan 02-05):
    - Button with Upload icon: "Upload Document"
    - `onClick` is a no-op with comment `// Wired by Plan 02-05 (DocumentUploader)`
    - Only show if user has admin role or upload permission

    Document list:
    - For each doc in meetingDocs:
      - Row with: FileText icon + file_name (flex-1) + uploaded_at date (text-xs, text-text-muted) + Download button
      - Download button: placeholder for now. Add comment `// Full download via storage.from().download() wired in Plan 02-05 (DocumentUploader)`.

    ---

    **Loading state:**
    Show Skeleton components while isLoading.

    **Permissions imports needed:**
    ```typescript
    import { canScheduleMeeting, canEditActionItems, canUnlockMinutes } from '../../lib/committees/permissions';
    ```

    **Supabase import:**
    ```typescript
    import { supabase } from '../../utils/supabaseClient';
    ```
  </action>
  <acceptance_criteria>
    - `src/pages/committees/MeetingDetail.tsx` exists
    - `grep -c "export default function MeetingDetail" src/pages/committees/MeetingDetail.tsx` returns 1
    - `grep -c "useParams" src/pages/committees/MeetingDetail.tsx` returns 1
    - `grep -c "useMemo" src/pages/committees/MeetingDetail.tsx` returns >= 3
    - `grep -c "canEditActionItems\|canUnlockMinutes\|canScheduleMeeting" src/pages/committees/MeetingDetail.tsx` returns >= 3 (imports + usages)
    - `grep -c "supabase" src/pages/committees/MeetingDetail.tsx` returns >= 2 (import + usage in status cycle/unlock)
    - `grep -c "isLocked\|Locked\|locked" src/pages/committees/MeetingDetail.tsx` returns >= 3 (D-19 lock display)
    - `grep "Meeting not found" src/pages/committees/MeetingDetail.tsx` returns 1
    - `grep -c "cycleStatus\|cycle.*status" src/pages/committees/MeetingDetail.tsx` returns >= 1 (D-12 status toggle)
    - `grep -c "refreshData" src/pages/committees/MeetingDetail.tsx` returns >= 2 (called after status cycle and unlock)
    - `grep "Wired by Plan 02-05" src/pages/committees/MeetingDetail.tsx` returns >= 3 (comments marking agenda, minutes, document placeholders for wiring)
    - Contains ZERO occurrences of `getPublicUrl` (Pitfall 3)
    - Contains ZERO raw color classes (except amber lock badge which has no semantic token)
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>MeetingDetail page renders all 5 stacked sections. Minutes section is read-only display. Action item status badges are click-to-cycle. Agenda view mode is functional with placeholder edit button. Document list renders with placeholder upload. All interactive components (MinutesEditor, AgendaEditor, DocumentUploader) are wired by Plan 02-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Supabase PostgreSQL | Action item status updates and minutes unlock cross this boundary via supabase client |
| client -> Supabase Storage | Document download crosses this boundary (placeholder in this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | Minutes unlock (meetings UPDATE) | mitigate | RLS meetings_update policy (Plan 02-01) blocks UPDATE when locked. Unlock button only renders if canUnlockMinutes returns true and calls the SECURITY DEFINER unlock_meeting_minutes RPC which has its own role check. |
| T-02-07 | Tampering | Action item status cycle | mitigate | RLS action_items_write restricts UPDATE to admin roles. UI only shows clickable badge if canEditActionItems returns true. |
| T-02-08 | Information Disclosure | MeetingDetail | accept | All meeting data readable by all authenticated users per meetings_select RLS policy. No sensitive data beyond what's in the committee scope. |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run lint` exits 0
- Page renders at /committees/:id/meetings/:meetId route
- Minutes section shows read-only text with lock badge when applicable
- Status badge click cycles action item status
- Placeholder comments reference "Wired by Plan 02-05" in agenda, minutes, and documents sections
</verification>

<success_criteria>
1. MeetingDetail renders all 5 sections in stacked order per D-17
2. Minutes displayed as read-only text with amber Locked badge when Completed + 7 days
3. Admin sees Unlock button on locked meetings (calls unlock_meeting_minutes RPC)
4. Action item status badges are clickable and cycle Pending->InProgress->Completed->Pending
5. Status cycle persists to Supabase and refreshes data
6. Document list renders with placeholder upload
7. Invalid meeting ID shows not-found UI
8. `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-committee-management/02-03-SUMMARY.md`
</output>
